{
    "dashboard_url": "wss://dashboard.example.com/api/ws/agent",
    "agent_token": "PASTE_TOKEN_FROM_DASHBOARD_HERE",
    "agent_id": "",
    "push_interval": 30,
    "log_level": "INFO",

    "_comment_relay": "Local API relay (§15). The agent forwards dashboard HTTP requests to the box's own REST API. On OPNsense it auto-provisions its own key (dedicated 'orbit' user) — leave the creds empty. Set them only to use a hand-made key instead, or set relay_provision=false to disable auto-provisioning.",
    "opnsense_api_url": "https://127.0.0.1:4444",
    "opnsense_api_key": "",
    "opnsense_api_secret": "",
    "relay_provision": true
}
